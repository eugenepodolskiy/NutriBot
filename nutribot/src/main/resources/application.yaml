spring:
  application:
    name: nutribot
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://localhost:5432/nutribot}
    username: ${SPRING_DATASOURCE_USERNAME:nutribot}
    password: ${SPRING_DATASOURCE_PASSWORD:nutribot}
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
  flyway:
    enabled: true
    locations: classpath:db/migration

telegram:
  bot:
    token: ${TELEGRAM_BOT_TOKEN}
    username: ${TELEGRAM_BOT_USERNAME:}

gemini:
  api:
    key: ${GEMINI_API_KEY}

management:
  endpoints:
    web:
      exposure:
        include: health

server:
  port: ${PORT:8080}
